name: thingsboard
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.17.5 <3.0.0"

dependencies:
  flutter:
    sdk: flutter


  cupertino_icons: ^1.0.2
  flutter_svg: ^1.1.5
  get: ^4.6.5
  dio: ^4.0.6
  shared_preferences: ^2.0.15
  get_it: ^7.2.0
  web_socket_channel: ^2.2.0
  responsive_framework: ^0.2.0
  thingsboard_pe_client: ^1.0.4
dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:

  uses-material-design: true
  assets:
    - assets/images/
